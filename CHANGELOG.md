# Changelog

## 2.0.0

- Initial LangGraph-based release.
- Keeps the same staged planning, autorun, and verification workflow as `Agent_Autorun_Protocol`.
- Replaces the ACL-X file runtime with a local LangGraph runtime and checkpoint store.
- Adds automatic local setup for the LangGraph environment.
