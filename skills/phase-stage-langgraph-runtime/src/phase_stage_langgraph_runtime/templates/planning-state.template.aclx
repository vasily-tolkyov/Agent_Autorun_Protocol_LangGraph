kind=phase_stage_langgraph_plan
protocolVersion={{protocol_version}}
runtimeTier=t0
runId={{run_id}}
title={{title}}
generatedAt={{generated_at}}
planningMode={{planning_mode}}

project.root={{project_root}}
planning.root={{planning_root}}
runtime.root={{runtime_root}}

approval.status={{approval_status}}
phase.current={{current_phase_id}}
phase.executable={{current_executable_phase_id}}
phase.firstPending={{first_pending_phase_id}}
phase.ready={{ready_phase_ids_json}}
phase.pending={{pending_phase_ids_json}}
phase.items={{phase_items_json}}

stage.queue={{current_stage_queue_json}}

artifacts.protocol={{protocol_path}}
artifacts.phaseIndex={{phase_index_path}}
artifacts.taskIntake={{task_intake_path}}
artifacts.taskContext={{task_context_path}}

inv.phaseTopologyComplete=true
inv.stageDetailRolling=true
inv.noPendingExecution=true
inv.langgraphAuthority=true
