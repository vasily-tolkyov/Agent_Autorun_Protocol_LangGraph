kind=phase_stage_langgraph_runtime
protocolVersion={{protocol_version}}
runtimeTier={{runtime_mode}}
runId={{run_id}}
title={{title}}
graphId={{graph_id}}
assistantId={{assistant_id}}
threadId={{thread_id}}

project.root={{project_root}}
planning.root={{planning_root}}
runtime.root={{runtime_root}}

queue.items={{queue_items_json}}
queue.cursor={{queue_cursor}}

phase.current={{current_phase_id}}
stage.current={{current_stage_id}}
stage.path={{current_stage_path}}
stage.state={{stage_state}}
stage.next={{next_action}}
stage.blocker={{blocker}}

audit.passStreak={{audit_pass_streak}}
audit.failStreak={{audit_fail_streak}}
verify.latest={{latest_verification}}
loop.round={{round}}
loop.phase={{verification_phase}}

resume.thread={{thread_id}}
resume.checkpoint={{checkpoint_id}}
